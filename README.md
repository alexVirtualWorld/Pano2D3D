# Pano2D3D
Panoramic image to generate 3D scene
If the "(generate a scene)" button still shows a waiting state after uploading an image, it's because the cached JS file hasn't been fully downloaded. Refreshing the page should resolve this.
WASD controls the direction of travel, and QE controls elevation control.
